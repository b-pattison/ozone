<nav class="navbar navbar-expand-lg bg-secondary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Ozone</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
      </ul>
      <form class="d-flex ms-auto" role="search" action="<%= root_path %>" method="get">
        <input name="zip_code" class="form-control me-2" type="search" placeholder="Enter ZIP code" aria-label="ZIP code" value="<%= @zip_code %>" pattern="\d{5}" title="5-digit ZIP code" />
        <button class="btn btn-outline-dark" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>

